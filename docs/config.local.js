// LOKALNA KONFIGURACJA — nie commitować do repo (jeśli kiedyś trafi do gita, dopisz do .gitignore).
// Ten klucz Google Maps API powinien mieć w Google Cloud Console ustawione:
//   - Application restrictions: HTTP referrers → http://localhost/* + http://127.0.0.1/*
//   - API restrictions: Maps JavaScript API + Routes API + Geocoding API
// Bez tych restrictions klucz jest narażony na nadużycie, jeśli wycieknie.
window.GMAPS_API_KEY = 'AIzaSyDTH-d-asyE-6kRkjFTeW76voaR370hgL0';
